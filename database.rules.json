{
    "rules": {
        "users": {
            "$user_id": {
                ".write": "$user_id === auth.uid",
              	".read": "$user_id === auth.uid"
            }
        },
        "data": {
            ".read": "auth != null",
            ".write": "auth != null"
    }
}
}
